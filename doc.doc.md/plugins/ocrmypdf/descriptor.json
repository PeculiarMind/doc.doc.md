{
  "name": "ocrmypdf",
  "version": "1.1.0",
  "description": "A plugin that runs OCR on PDF and image files (JPEG, PNG, TIFF, BMP, GIF) using OCRmyPDF, extracting plain text as JSON output.",
  "active": false,
  "commands": {
    "process": {
      "description": "Run OCR on a PDF or image file and return extracted text.",
      "command": "main.sh",
      "input": {
        "filePath": {
          "type": "string",
          "description": "Path to the PDF or image file to be processed. Supported MIME types: application/pdf, image/jpeg, image/png, image/tiff, image/bmp, image/gif.",
          "required": true
        },
        "imageDpi": {
          "type": "integer",
          "description": "DPI to use when processing image inputs. Defaults to 300 if not provided.",
          "required": false,
          "default": 300
        }
      },
      "output": {
        "ocrText": {
          "type": "string",
          "description": "Full plain-text content extracted by OCR."
        }
      }
    },
    "installed": {
      "description": "Check if ocrmypdf is available on the system.",
      "command": "installed.sh",
      "output": {
        "installed": {
          "type": "boolean",
          "description": "true if ocrmypdf is available, false otherwise."
        }
      }
    },
    "install": {
      "description": "Install ocrmypdf if not already available.",
      "command": "install.sh",
      "output": {
        "success": {
          "type": "boolean",
          "description": "true if ocrmypdf is available after the installation attempt."
        },
        "message": {
          "type": "string",
          "description": "Human-readable status message describing the installation result."
        }
      }
    }
  }
}
